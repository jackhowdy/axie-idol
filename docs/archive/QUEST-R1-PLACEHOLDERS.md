# R1 Quest placeholders

- **Starter stickers/previews** (`buba`, `olek`, `puffy`): temporarily reuse Kotaro PNG under `public/previews/` and `public/stickers/`. Replace with official starter art when available. No GLBs — PNG path only.
- **Agonia Echo** (`agonia-echo`): custom camera sticker + black preview (generated R1 art). L24 capstone. Legacy id `villain` remaps on read.
- **Numeric idol faces** (`4154`, `4155`, `4156`, `991`, `35`, `1367`, `2660`): use Axie CDN / `/api/image/{id}` previews; no kit GLBs.
- **Seeds/burns**: still paused (`SEED_POSTS` gate; burns UI soft-hidden). Seed auto-refresh not revived.
