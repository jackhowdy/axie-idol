# Axie Idol — takeover audit (2026-09-05)

Claude took over from the Grok handover pack (`Desktop/Axie-Idol-Vibeathon-Handover`).
Working repo: `Desktop/axie-idol` (this folder). Baseline commit = snapshot as received.

## Verified today
- Raw `vite build` succeeds; `npm run build` (tsc && vite build) FAILS with 9 TS errors
  (unused vars, `never` narrowing on CAST_MASCOTS, Waypoint `scopes` typed as string[]).
- `node server.mjs` serves dist + APIs on :5174. Feed, Board, Create render at 375px.
- Inventory/ownership path (`graphql-gateway.axieinfinity.com` via curl) is blocked by a
  Cloudflare bot challenge from this machine. Official gateway
  `api-gateway.skymavis.com/graphql/axie-marketplace` accepts `x-api-key` and has CORS `*`.
- `metadata.axieinfinity.com/axie/{id}` works (200).
- `data/uploads/` absent in snapshot -> every post image 404s. posts.json holds test data.
- Bundle: spineSticker chunk 4.9 MB (mixer data), tripp.glb 7.1 MB, dist 38 MB.

## Gaps vs rules / rubric
- Rules 4: basic play must not require wallet/account -> OK (guest path).
- Get-started guide warns against unnecessary live service / custom backend and wants
  start -> challenge -> outcome -> retry. Current app is feed + quest ladder; no bounded
  challenge a lone judge can complete in 5 minutes.
- Rules 9: harassment/sexual content prohibited -> UGC photo feed has no moderation.
- Rules 4: AI-generated assets must be disclosed -> Agonia Echo (AI art), starter
  placeholders (Kotaro reused for Buba/Olek/Puffy).
- Hosting: Node + JSON files + local uploads needs a persistent Node host with HTTPS
  that stays up Sep 22-28. trycloudflare tunnel is not acceptable for judging.

## Key dates (UTC)
- Register by 2026-09-07 13:00 UTC (21:00 HKT).
- Round 1 submissions 2026-09-08 13:00 -> 2026-09-21 13:00 UTC.
- Judging 09-22..28; finalists 09-29; Round 2 10-04..31; results 11-05.
